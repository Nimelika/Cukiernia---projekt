<local:SingleViewBase x:Class="DesktopApp.Views.SingleObjectViews.CreateModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="/Views/TabStyleResources.xaml"/>
    </UserControl.Resources>

    <Grid Style="{StaticResource MainBackground}">
        <Grid>
            <Grid Style="{StaticResource IvoryBackground}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="270"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>

                </Grid.ColumnDefinitions>
                <Label Content="Module Name:" Grid.Column="0"/>
                <Label Content="Description:" Grid.Column="2"/>
                <TextBox Grid.Column="1" Style="{StaticResource ItemInError}" Text="{Binding Path=ModuleName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnDataErrors=True}"/>
                <TextBox Grid.Column="3" Style="{StaticResource ItemInError}" Text="{Binding Path=Description, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnDataErrors=True}"/>
            </Grid>
        </Grid>
    </Grid>
</local:SingleViewBase>



