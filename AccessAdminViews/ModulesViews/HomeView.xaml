<UserControl x:Class="DesktopApp.Views.AccessAdminViews.ModulesViews.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        <!-- HEADER -->
        
        <TextBlock
            Text="{Binding WelcomeText}"
            FontSize="32"
            FontWeight="Bold"
            Margin="10"
            HorizontalAlignment="Center"
            Foreground="#4A4947"/>

       
        <!-- DASHBOARD -->
       
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,20">

            <Border Background="#FFFFFF"
                    Padding="20"
                    CornerRadius="10"
                    Margin="10">
                <StackPanel>
                    <TextBlock Text="New Orders"
                               FontSize="14"
                               Foreground="#666"/>
                    <TextBlock Text="{Binding NewOrdersCount}"
                               FontSize="28"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Background="#FFFFFF"
                    Padding="20"
                    CornerRadius="10"
                    Margin="10">
                <StackPanel>
                    <TextBlock Text="New Quote Requests"
                               FontSize="14"
                               Foreground="#666"/>
                    <TextBlock Text="{Binding NewQuoteRequestsCount}"
                               FontSize="28"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

        </StackPanel>

        
        <!-- PERMISSIONS -->
       
        <GroupBox Grid.Row="2"
                  Header="Your access rights"
                  Padding="10">

            <ItemsControl ItemsSource="{Binding Permissions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Name}"
                                       VerticalAlignment="Center"/>

                            <!-- Has access -->
                            <TextBlock Grid.Column="1"
                                       Text="✔"
                                       FontSize="16"
                                       Foreground="#427A43"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding HasAccess,
                                           Converter={StaticResource BoolToVisibilityConverter}}"/>

                            <!-- No access -->
                            <TextBlock Grid.Column="1"
                                       Text="✖"
                                       FontSize="16"
                                       Foreground="#C3110C"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding HasAccess,
                                           Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </GroupBox>
    </Grid>
</UserControl>
