<local:SingleViewBase
    x:Class="DesktopApp.Views.SingleObjectViews.SingleObjectDictionaries.Order.DeleteOrderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DesktopApp.Views">

    <UserControl.Resources>
        <ResourceDictionary Source="/Views/TabStyleResources.xaml" />
    </UserControl.Resources>

    <Grid Margin="10">
        <StackPanel>

            <TextBlock Text="Delete Order" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

            <TextBlock Text="Are you sure you want to delete this order?" Margin="0,0,0,10"/>

            <TextBlock Text="{Binding OrderId}" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding Customer}" Margin="0,0,0,15"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Delete"
                        Command="{Binding DeleteCommand}"
                        Margin="0,0,10,0"/>
                <Button Content="Cancel"
                        Command="{Binding CancelCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</local:SingleViewBase>

