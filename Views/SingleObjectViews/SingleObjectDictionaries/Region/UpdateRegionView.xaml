<local:SingleViewBase
    x:Class="DesktopApp.Views.SingleObjectViews.UpdateRegionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DesktopApp.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary Source="/Views/TabStyleResources.xaml" />
    </UserControl.Resources>

    <Grid>
        <Grid>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="45" />
                    <RowDefinition Height="45" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="300" />
                </Grid.ColumnDefinitions>
                <Label
                    Grid.Row="1"
                    Content="Country Name:"
                    Style="{StaticResource SemiBoldLabelStyle}" />
                <Label
                    Grid.Column="0"
                    Content="Region Name:"
                    Style="{StaticResource SemiBoldLabelStyle}" />
                <TextBox
                    Grid.Column="1"
                    Style="{StaticResource StandardTextBoxStyle}"
                    Text="{Binding Path=RegionName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnDataErrors=True}" />
                <ComboBox
                    Grid.Row="1"
                    Grid.Column="1"
                    DisplayMemberPath="Value"
                    ItemsSource="{Binding CountryComboBoxItems}"
                    SelectedValue="{Binding Path=Country, Mode=TwoWay}"
                    SelectedValuePath="Key"
                    Style="{StaticResource StandardComboBoxStyle}" />
            </Grid>
        </Grid>
    </Grid>
</local:SingleViewBase>


