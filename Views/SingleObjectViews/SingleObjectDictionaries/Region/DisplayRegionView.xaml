<local:SingleViewBase
    x:Class="DesktopApp.Views.SingleObjectViews.DisplayRegionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DesktopApp.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary Source="/Views/TabStyleResources.xaml" />
    </UserControl.Resources>

    <Grid>
        <Grid>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition Height="40" />
                    <RowDefinition Height="100" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="300" />
                </Grid.ColumnDefinitions>
                <Label
                    Grid.Row="1"
                    Content="Country Name:"
                    Style="{StaticResource SemiBoldLabelStyle}" />
                <Label Content="Region Name:" Style="{StaticResource SemiBoldLabelStyle}" />
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Style="{StaticResource StandardTextColumnStyle}"
                    Text="{Binding RegionName}" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Style="{StaticResource StandardTextColumnStyle}"
                    Text="{Binding CountryName}" />
                <StackPanel Grid.Row="2" Style="{StaticResource DisplayDeleteStackPanelStyle}">
                    <Button
                        Command="{Binding ReturnCommand}"
                        Content="Return"
                        Style="{StaticResource StandardStackPanelButtonStyle}" />
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</local:SingleViewBase>



